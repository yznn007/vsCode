#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    double H;
    cin >> H;
    H =(H-100)*1.8;
    cout << fixed << setprecision(1) << H;
    return 0;
}